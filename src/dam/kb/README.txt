In order to initialize the NotreDAM knowledge base, it is necessary to
execute the following script:

    python init_kb.py -t

The "-t" switch (optional) will also create some test classes and
objects.
