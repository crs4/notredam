[MQUEUE]
# Numeric ip or dns name of rabbitMQ instance
server_xmp = mediadart.xmp.xmp_embedder.start_server
server_mprocessor = dam.mprocessor.processors.start_server


[STORAGE]
# storage for resources shared 
cache_dir = /home/clem/notredam_repository

# raise exception if passed paths are not in the cache
check_in_cache = no
